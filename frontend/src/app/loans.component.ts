import {Component,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {ApiService,Loan} from './api.service';import {AuthService} from './auth.service';@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<h2>Loans</h2><div class="card"><form (ngSubmit)="apply()"><select [(ngModel)]="form.loanType" name="type"><option *ngFor="let x of types">{{x}}</option></select><input type="number" [(ngModel)]="form.requestedAmount" name="amount" placeholder="Amount" min="1000"><input type="number" [(ngModel)]="form.tenureMonths" name="tenure" placeholder="Tenure (months)" min="1" max="360"><input type="number" [(ngModel)]="form.annualInterestRate" name="rate" placeholder="Interest rate"><input [(ngModel)]="form.purpose" name="purpose" placeholder="Purpose"><button>Apply for Loan</button></form></div><div class="card" *ngFor="let l of items"><b>{{l.loanType}} — {{l.applicationReference}}</b><div>₹{{l.requestedAmount|number}} · {{l.tenureMonths}} months · {{l.status}}</div><small>{{l.purpose}}</small></div><p *ngIf="message">{{message}}</p>`,styles:[`form{display:grid;gap:10px}input,select{padding:10px}button{padding:10px}.card{margin:12px 0}`]})export class LoansComponent{api=inject(ApiService);auth=inject(AuthService);items:Loan[]=[];types=['PERSONAL','HOME','AUTO','EDUCATION','BUSINESS'];message='';form:any={loanType:'PERSONAL',requestedAmount:1000,tenureMonths:12,annualInterestRate:0,purpose:''};ngOnInit(){this.load()}private customer(){return this.auth.customerNumber()}load(){this.api.loans(this.customer()).subscribe({next:x=>this.items=x,error:()=>this.message='Unable to load loans'})}apply(){this.api.applyLoan({...this.form,customerNumber:this.customer()}).subscribe({next:x=>{this.items=[x,...this.items];this.message='Loan application submitted'},error:()=>this.message='Unable to apply for loan'})}}