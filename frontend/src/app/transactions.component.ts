import {Component,inject} from '@angular/core'; import {CommonModule} from '@angular/common'; import {FormsModule} from '@angular/forms'; import {ApiService,Transaction} from './api.service'; import {AuthService} from './auth.service';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<section class="page"><div class="section-head"><div><p class="eyebrow">TRANSACTIONS</p><h1>Transaction history</h1></div><button (click)="load()">Refresh</button></div>@if(error){<p class="error">{{error}}</p>}@if(success){<p class="success">{{success}}</p>}<div class="card form-card"><h3>New transaction</h3><div class="form-grid"><label>Source account<input [(ngModel)]="form.sourceAccountNumber"></label><label>Destination account<input [(ngModel)]="form.destinationAccountNumber"></label><label>Type<select [(ngModel)]="form.type"><option>TRANSFER</option><option>DEPOSIT</option><option>WITHDRAWAL</option></select></label><label>Amount<input type="number" min="0.01" [(ngModel)]="form.amount"></label><label>Currency<input maxlength="3" [(ngModel)]="form.currency"></label><label>Description<input [(ngModel)]="form.description"></label></div><button (click)="submit()">Submit transaction</button></div><div class="table-wrap"><table><thead><tr><th>Reference</th><th>Type</th><th>Amount</th><th>Status</th><th>Date</th></tr></thead><tbody>@for(t of transactions;track t.transactionReference){<tr><td>{{t.transactionReference}}</td><td>{{t.type}}</td><td>{{t.currency}} {{t.amount}}</td><td>{{t.status}}</td><td>{{t.createdAt | date:'short'}}</td></tr>}</tbody></table></div></section>`,})
export class TransactionsComponent {private api=inject(ApiService);private auth=inject(AuthService);transactions:Transaction[]=[];error='';form:any={type:'TRANSFER',amount:null,currency:'INR',sourceAccountNumber:'',destinationAccountNumber:'',description:''};customer='';success='';constructor(){this.customer=this.auth.customerNumber();this.load()}load(){if(!this.customer)return;this.api.transactions(this.customer).subscribe({next:r=>this.transactions=r,error:()=>this.error='Unable to load transactions.'})}submit(){this.error='';this.success='';if(!this.form.amount){this.error='Enter a valid amount.';return}if((this.form.type==='TRANSFER'||this.form.type==='WITHDRAWAL')&&!this.form.sourceAccountNumber){this.error='Source account is required for this transaction type.';return}if((this.form.type==='TRANSFER'||this.form.type==='DEPOSIT')&&!this.form.destinationAccountNumber){this.error='Destination account is required for this transaction type.';return}if(this.form.type==='TRANSFER'&&this.form.sourceAccountNumber===this.form.destinationAccountNumber){this.error='Source and destination accounts must be different.';return}this.api.createTransaction({...this.form,customerNumber:this.customer,idempotencyKey:'WEB-'+Date.now(),currency:this.form.currency.toUpperCase()}).subscribe({next:()=>{this.load();this.success='Transaction submitted successfully.'},error:e=>this.error=e?.error?.message||'Transaction failed.'})}}