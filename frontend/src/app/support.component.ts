import {Component,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {ApiService,Ticket} from './api.service';import {AuthService} from './auth.service';@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<h2>Support</h2><div class="card"><form (ngSubmit)="create()"><select [(ngModel)]="form.requestType" name="type"><option>SERVICE_REQUEST</option><option>COMPLAINT</option></select><select [(ngModel)]="form.priority" name="priority"><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>URGENT</option></select><input [(ngModel)]="form.subject" name="subject" placeholder="Subject" maxlength="150" required><textarea [(ngModel)]="form.description" name="description" placeholder="Description" maxlength="2000" required></textarea><button>Create Ticket</button></form></div><div class="card" *ngFor="let t of items"><b>{{t.subject}}</b><div>{{t.ticketReference}} · {{t.priority}} · {{t.status}}</div><p>{{t.description}}</p></div><p *ngIf="message">{{message}}</p>`,styles:[`form{display:grid;gap:10px}input,select,textarea{padding:10px}button{padding:10px}.card{margin:12px 0}`]})export class SupportComponent{api=inject(ApiService);auth=inject(AuthService);items:Ticket[]=[];message='';form:any={requestType:'SERVICE_REQUEST',priority:'MEDIUM',subject:'',description:''};ngOnInit(){this.load()}private customer(){return this.auth.customerNumber()}load(){this.api.tickets(this.customer()).subscribe({next:x=>this.items=x,error:()=>this.message='Unable to load tickets'})}create(){this.api.createTicket({...this.form,customerNumber:this.customer()}).subscribe({next:x=>{this.items=[x,...this.items];this.message='Ticket created';this.form={requestType:'SERVICE_REQUEST',priority:'MEDIUM',subject:'',description:''}},error:()=>this.message='Unable to create ticket'})}}