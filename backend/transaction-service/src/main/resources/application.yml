spring:
  kafka:
    bootstrap-servers: localhost:9092
  application:
    name: transaction-service
  datasource:
    url: jdbc:postgresql://localhost:5432/finbank
    username: finbank
    password: finbank
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info
spring:
  kafka:
    bootstrap-servers: localhost:9092
